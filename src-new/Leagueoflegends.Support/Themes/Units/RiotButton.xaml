<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:units="clr-namespace:Leagueoflegends.Support.UI.Units">

    <LinearGradientBrush x:Key="RiotButtonBorderBrush" StartPoint="0.5,0" EndPoint="0.5,1">
        <GradientStop Color="#C8AA6E" Offset="0"/>
        <GradientStop Color="#795c28" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="RiotButtonBorderBrushHover" StartPoint="0.5,0" EndPoint="0.5,1">
        <GradientStop Color="#f0e6d7" Offset="0"/>
        <GradientStop Color="#c99d3d" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="RiotButtonBackgroundHover" StartPoint="0.5,0" EndPoint="0.5,1">
        <GradientStop Color="#1e232a" Offset="0"/>
        <GradientStop Color="#443e2e" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="RiotButtonBorderBrushPressed" StartPoint="0.5,0" EndPoint="0.5,1">
        <GradientStop Color="#785a28" Offset="0"/>
        <GradientStop Color="#c8aa6e" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="RiotButtonBackgroundPressed" StartPoint="0.5,0" EndPoint="0.5,1">
        <GradientStop Color="#373121" Offset="0"/>
        <GradientStop Color="#1e2328" Offset="1"/>
    </LinearGradientBrush>

    <Style TargetType="{x:Type units:RiotButton}">
        <Setter Property="Height" Value="30"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="#CDBE91"/>
        <Setter Property="Background" Value="#1E2328"/>
        <Setter Property="BorderBrush" Value="{StaticResource RiotButtonBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type units:RiotButton}">
                    <Grid>
                        <Border x:Name="BorderElement"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0">
                            <Grid>
                                <ContentPresenter x:Name="ContentPresenter"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"/>
                                <Border Background="Transparent"
                                        BorderBrush="#CC091119"
                                        BorderThickness="1"
                                        Margin="0"/>
                            </Grid>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ContentPresenter" Property="TextElement.Foreground" Value="#f0e6d2"/>
                            <Setter TargetName="BorderElement" Property="BorderBrush" Value="{StaticResource RiotButtonBorderBrushHover}"/>
                            <Setter TargetName="BorderElement" Property="Background" Value="{StaticResource RiotButtonBackgroundHover}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ContentPresenter" Property="TextElement.Foreground" Value="#e4caa5"/>
                            <Setter TargetName="BorderElement" Property="BorderBrush" Value="{StaticResource RiotButtonBorderBrushPressed}"/>
                            <Setter TargetName="BorderElement" Property="Background" Value="{StaticResource RiotButtonBackgroundPressed}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>